// import { Router } from "express";
// import locationRoute from "./Location/locationRoute.js";

const router = "sohan";

// const defaultRoutes = [
//     {
//         path: "/auth",
//         route: locationRoute,
//     },
// ];

// defaultRoutes.forEach((route) => {
//     router.use(route.path, route.route);
// });

export default router;
